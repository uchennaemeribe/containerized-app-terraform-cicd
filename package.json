{
  "name": "cicd-app",
  "version": "1.0.0",
  "description": "CI/CD containerized app",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "curl -f http://localhost:3000/health || exit 1"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
